---
title: "swarm list"
description: List all sessions across all repositories.
---

Shows all sessions from the global registry, spanning all repos.

## Usage

```bash
swarm list
```

Sessions are tracked in `~/.config/copilot-swarm/sessions.json`. Output shows session ID, name, repository path, status (active/finished), and creation timestamp.
